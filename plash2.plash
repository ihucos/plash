
os: zesty

apt: python3 wget squashfs-tools python3-setuptools tree

layer:
run:
easy_install3 pip
pip install wheel

layer:

bustcache:

run:
cd {{pwd}}
python3 setup.py bdist_wheel
pip install {{pwd}}/dist/*.whl
