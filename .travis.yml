language: generic
sudo: enabled

install:
  # install plash
  - sudo apt-get install python3 python3-pip
  - sudo pip3 install plash

#cache:
#  directories:
#    - ~/plashdata/mydata.tar

git:
  depth: 1

envs:
  - BUILD="--alpine python3 bash"
  - BUILD="--ubuntu python3"

script:
  - sudo plash-init
  - sudo plash-run $BUILD -- misc/runtests
