--from
ubuntu:eoan

--apt
gnupg
python3
fuse-overlayfs
gcc
make
curl

--layer

--run
#                                              PUT MASTER HERE
curl -Lf https://github.com/ihucos/plash/archive/dist.tar.gz | tar -xzC / --strip-components=1
cd /opt/plash/
make clean
make

--layer
--write-script /etc/profile.d/export_path.sh
export PATH

--# --write-script
--# /usr/bin/plash
--# sh -lc 'export PATH; exec /usr/local/bin/plash "$@"' -- "$@"
