#!/bin/sh
set -eu

test $# -eq 2 || {
  echo 'USAGE: import-url url image-id' >&2
  exit 2
}

url=$1
image_id=$2
tmpdir=$(mktemp --directory)
wget -q --show-progress $url -O $tmpdir/file
exec plash.import-tar $tmpdir/file $image_id
