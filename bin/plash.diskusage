#!/bin/sh
if [ "$(id -u)" != "0" ]; then
   echo "This script must be run as root" 1>&2
   exit 1
fi

printf '     Used: '
du -sh /var/lib/plash 2> /dev/null | awk '{print $1}'

printf 'Available: '
df -h /var/lib/plash | awk 'NR > 1 {print $4}'
