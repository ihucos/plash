#!/usr/bin/env python3

# usage: plash-runfile container [arg1 [arg2 [arg3 ...]]]

import os, sys
from plashlib.utils import handle_help_flag
handle_help_flag()
os.execlp(
    'plash-do',
    'plash-do',
    'runp',
    '--include',
    sys.argv[1],
    '--',
    *sys.argv[1:])
