#!/bin/sh

read -r -p "Delete all build data? [y/N] " response
case "$response" in
    [yY][eE][sS]|[yY]) 
        exec rm -rf /var/lib/plash/* # don't delete the dir itself because it could be a mount
        ;;
    *)
esac
