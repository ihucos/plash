#!/bin/sh

test $# -eq 1 || {
      echo "USAGE: plash.nodepath container" >&2
      exit 2
}

readlink --canonicalize-existing /var/lib/plash/links/$1 || {
      echo "No such container: $1" >&2
      exit 1
}
