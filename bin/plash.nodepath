#!/bin/sh

test $# -eq 1 || {
      echo "USAGE: plash.nodepath container" >&2
      exit 2
}

nodepath=$(readlink ${PLASH_DATA:-/var/lib/plash}/links/$1)
if ! [ $? -eq 0 ] || ! [ -d "$nodepath" ]; then
      echo "No such container: $1" >&2
      exit 1
fi
echo $nodepath
