#!/usr/bin/env python3
#
# usage: plash-run-cleanup

import os

lives_cache = {}
def lives(process):
    asdf

with open('/proc/mounts') a f:
    line = f.readline()
    if not line:
        sys.exit(0)
    mountpoint = line.split()[1]
    if not mountpoint.startswith('/run/plash-run-'):
        continue
