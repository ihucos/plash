#!/bin/sh

# usage: plash export-squashfs CONTAINER FILE
# export a container as squashfs

set -eu
  
test "$*" == --help && exec plash.help "$0"
test $# -eq 2 || {
  plash.help --usage "$0"
  exit 2
}

exec plash.with-mount "$1" mksquashfs . "$2"
